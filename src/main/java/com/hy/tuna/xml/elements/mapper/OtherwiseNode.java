package com.hy.tuna.xml.elements.mapper;

import com.hy.tuna.sql.Context;

public class OtherwiseNode extends DirectiveNode {

    @Override
    public boolean apply(Context context) {
        return super.apply(context);
    }
}
